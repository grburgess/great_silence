{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.py) ~/.local/bin/micromamba run -n galaticbot black -q \"$f\" >/dev/null 2>&1; ~/.local/bin/micromamba run -n galaticbot ruff check --fix -q \"$f\" >/dev/null 2>&1;; esac"
          }
        ]
      }
    ]
  }
}
